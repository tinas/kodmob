import { RefreshControl } from 'react-native'
import styled from 'styled-components'
import { compose, space } from 'styled-system'

const Refresh = styled(RefreshControl)(compose(space))

export default Refresh
